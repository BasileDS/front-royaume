/**
 * Shared Module
 * 
 * Ce module contient les ressources partagées dans toute l'application :
 * - Types communs
 * - Hooks génériques
 * - Composants UI réutilisables
 * - Utilitaires
 * 
 * Usage:
 * ```typescript
 * import { AsyncState, PaginationOptions } from '@/shared/types';
 * ```
 */

export * from './types';
